package backend_center

import org.junit.Test

class TestClass(var height: Double, var length: Double) {
    var perimeter = (height + length) / 2

    @Test
    fun testCreateClass() {
        println(1)
    }
}
